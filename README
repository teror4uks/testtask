Тест запускается при помощи скрипта run_test.sh.

Скрипт выполняет следующие шаги:

1. Проверяет есть виртуальное окружение для запуска теста (директория env)
1.1 Если окружения нет - оно создается
2. Окружение активируется и устанавливаются требуемые зависимости
3. Запускается тест с сохранением Junit отчета в файл report.xml
